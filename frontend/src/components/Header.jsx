import React from 'react'
import useAuth from '../hooks/useAuth'

const Header = () => {
    const { user } = useAuth()
    
    return (
        <div>
            
        </div>
    )
}

export default Header